def hello(name):
    if name != None and True:
        print "hello " + name
        return 42
    else:
        print "An error occured"
        return -1

names = ["Martin", "Zoé"]
count = 0
for name in names:
    count = count + hello(name)
names[count % 2] = str(count)
